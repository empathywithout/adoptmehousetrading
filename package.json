{
  "name": "adoptmehousetrading",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "parse": "node scripts/parse-catalog.mjs",
    "copy-data": "mkdir -p public/data && cp data/*.json public/data/",
    "generate": "node scripts/generate-house-pages.mjs",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "build": "npm run parse && npm run copy-data && npm run generate && npm run sitemap"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.6",
    "@upstash/redis": "^1.34.3",
    "ws": "^8.21.1"
  }
}
